# EX-000 — Executable Sprint: Foundation Health Check

**Session type:** EX (Executable — live system validation)  
**Run after:** RM-001 complete  
**Purpose:** Verify the foundation (health, auth, webhook) works against a running server

## Load Before Starting

- `docs/specs/sections/05-auth.md`
- `docs/specs/sections/06-api.md` §6.15

## Hurl Probes

```hurl
# EX-000: Health check
GET http://{{api_host}}/health
HTTP 200
[Asserts]
jsonpath "$.status" == "ok"
jsonpath "$.ts" isString

# EX-000: Auth rejection
GET http://{{api_host}}/api/projects
HTTP 401
[Asserts]
jsonpath "$.code" == "UNAUTHORIZED"

# EX-000: Auth acceptance (requires valid Clerk token)
GET http://{{api_host}}/api/projects
Authorization: Bearer {{clerk_test_jwt}}
HTTP 200
```

## Definition of Done

- [ ] All 3 probes pass against live server
- [ ] Server start time < 5 seconds
