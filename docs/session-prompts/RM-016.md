# RM-016 — Prescription + Treatment Plan UI

**Session type:** RM  
**Prereqs:** RM-015 (hypothesis review UI)

## Load Before Starting

- `docs/specs/sections/10-frontend.md` §prescription flow
- `docs/specs/sections/06-api.md` §6.6, §6.7

## TCs to Satisfy

| TC | Description | Target |
|----|-------------|--------|
| TC-013 | Generate prescription (frontend trigger) | PASS |
| TC-014 | Generate treatment plan (frontend trigger) | PASS |

## Tasks

1. **Prescriptions page** (`app/(auth)/assessments/[id]/prescriptions/page.tsx`)
   - "Generate Prescriptions" button (triggers POST)
   - Lists `PrescriptionCard`: remedy title, rationale, priority badge, effort
   - "Generate Treatment Plan" CTA after prescriptions loaded

2. **Treatment Plan page** (`app/(auth)/assessments/[id]/treatment-plan/page.tsx`)
   - "Generate Plan" button (triggers POST)
   - Phase-by-phase timeline display
   - "Generate Final Report" CTA

3. **Mutations**:
   - `useGeneratePrescriptions()` — POST + navigate
   - `useGenerateTreatmentPlan()` — POST + navigate

## Definition of Done

- [ ] Generate Prescriptions button → loading state → prescription list renders
- [ ] Prescriptions display with priority (1=High, 2=Medium, 3=Low) color coding
- [ ] Treatment plan phases render in timeline format
- [ ] Playwright E2E: TC-013 generate → display prescriptions
