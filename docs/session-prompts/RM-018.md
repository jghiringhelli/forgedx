# RM-018 — Knowledge Base Admin Endpoints

**Session type:** RM  
**Prereqs:** RM-003 (seed), RM-004 (auth)

## Load Before Starting

- `docs/specs/sections/06-api.md` §6.9 (knowledge base endpoints)
- `docs/specs/sections/13-knowledge-base.md`

## TCs to Satisfy

| TC | Description | Target |
|----|-------------|--------|
| TC-006 | View knowledge base | PASS |

## Tasks

1. **Route handlers** (`routes/knowledge.ts`)
   - `GET /knowledge/pathologies` — list all (public read, no auth needed in MVP)
   - `GET /knowledge/pathologies/:code` — single pathology with scoring rules
   - `GET /knowledge/remedies` — list all remedies
   - `GET /knowledge/remedies/:code` — single remedy with implementation steps

2. **Hurl probes** (`tests/hurl/knowledge.hurl`)

3. **Frontend**: knowledge base browse page (optional in MVP — link from report page)

## Definition of Done

- [ ] GET /knowledge/pathologies → 200 with all 29 pathologies
- [ ] GET /knowledge/remedies → 200 with all 20 remedies
- [ ] TC-006: PASS
