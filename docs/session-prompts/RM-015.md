# RM-015 — Hypothesis Review UI

**Session type:** RM  
**Prereqs:** RM-014 (dashboard + polling)

## Load Before Starting

- `docs/specs/sections/10-frontend.md` §hypothesis review flow
- `docs/specs/sections/06-api.md` §6.5 (hypothesis endpoints)

## TCs to Satisfy

| TC | Description | Target |
|----|-------------|--------|
| TC-012 | Confirm/discard hypothesis | PASS (frontend) |

## Tasks

1. **Hypotheses page** (`app/(auth)/assessments/[id]/hypotheses/page.tsx`)
   - Lists all hypotheses (pending, confirmed, discarded)
   - `HypothesisCard` component: title, explanation, confidence badge, excerpts
   - "Confirm" / "Discard" buttons with `data-testid`

2. **Mutations**:
   - `useConfirmHypothesis()` — PATCH + invalidate hypotheses query
   - `useDiscardHypothesis()` — PATCH + invalidate

3. **Confidence badge** colors: LOW=gray, MEDIUM=amber, HIGH=green

4. **CTA**: "Generate Prescriptions" button (disabled until ≥1 confirmed)

## Definition of Done

- [ ] Confirm button triggers PATCH + UI updates optimistically
- [ ] Discard button triggers PATCH + card moves to "discarded" section
- [ ] "Generate Prescriptions" disabled with 0 confirmed
- [ ] Playwright E2E: TC-012 full flow (confirm → generate prescription CTA enabled)
