# RM-013 — Funnel: Mini-Assessment + Lead Capture

**Session type:** RM  
**Prereqs:** RM-003 (seed), RM-004 (auth working)

## Load Before Starting

- `docs/specs/sections/11-funnel.md` (full — funnel map, 3 mini-Qs, CTA, lead capture)
- `docs/specs/sections/06-api.md` §6.14 (funnel endpoints)
- `docs/specs/sections/04-schema.md` MiniAssessmentLead model

## TCs to Satisfy

| TC | Description | Target |
|----|-------------|--------|
| TC-018 | Public mini-assessment (funnel entry) | PASS |
| TC-019 | Funnel → full assessment conversion | PASS |

## Tasks

1. **Funnel service** (`domain/funnel/funnel.service.ts`)
   - `submitMiniAssessment(answers[3], email?)`:
     - Deterministic teaser scoring (3 questions → 0–3 score)
     - Creates `MiniAssessmentLead` if email provided (upsert on email)
     - Returns `{ teaserScore, teaserLabel, cta: { skoolUrl, workshopUrl } }`
   - `convertLead(leadId, clerkUserId)` — updates lead with `convertedAt` + `clerkUserId`

2. **3 Mini-Assessment Questions** (hardcoded from §11):
   - Q1: "Does your team have a CLAUDE.md or spec index?" → maps to P-001
   - Q2: "Do you have automated tests covering >70% of your codebase?" → maps to P-004
   - Q3: "Are your architecture decisions documented as ADRs?" → maps to P-011
   (exact question text from §11)

3. **Teaser score label**:
   - 0 → "Critical gaps detected" (red)
   - 1 → "Significant gaps" (amber)
   - 2 → "Some progress" (yellow)
   - 3 → "Good foundation" (green)

4. **Route handlers** (`routes/funnel.ts`) — NO `requireAuth`
   - `POST /funnel/mini-assessment` — public
   - `POST /funnel/leads` — public (optional lead capture)
   - `POST /funnel/convert` — auth required (on Clerk sign-in)

5. **Landing page** (`apps/web/src/app/(public)/page.tsx`)
   - Hero with GS methodology intro
   - 3-question mini-assessment form
   - Teaser score reveal animation
   - Skool CTA + Workshop CTA
   - Email capture (optional) before auth gate

6. **Conversion hook** (`apps/web/src/app/(auth)/dashboard/page.tsx`)
   - On first load after sign-up: call `POST /funnel/convert` with `leadId` from localStorage

## Definition of Done

- [ ] TC-018: POST /funnel/mini-assessment → 200 + { teaserScore, teaserLabel, cta } without auth
- [ ] TC-018: Email provided → MiniAssessmentLead row created (upsert)
- [ ] TC-018: Duplicate email → upsert (no 409 error)
- [ ] TC-019: After Clerk sign-in → lead `convertedAt` updated
- [ ] TC-019: Pre-filled survey answers have `source: "document"` (not "survey")
- [ ] Landing page CTA links use `NEXT_PUBLIC_SKOOL_COURSE_URL` env var
